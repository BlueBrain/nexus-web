import * as React from 'react';
export declare const resourcesWritePermissionsWrapper: (
  child: React.ReactNode,
  permissionPath: string
) => React.FunctionComponentElement<
  import('@bbp/react-nexus').AccessControlProps
>;
