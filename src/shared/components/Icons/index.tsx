import * as React from 'react';
import type from '../../../icons/type.svg';
import schema from '../../../icons/schema.svg';
import { Icon } from 'antd';

export const Type = <Icon component={type} />;
export const Schema = <Icon component={schema} />;
