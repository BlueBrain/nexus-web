import * as React from 'react';
import { Resource } from '@bbp/nexus-sdk/lib/types';
declare const ResourceMetadata: React.FC<{
  resource: Resource;
  orgLabel: string;
  projectLabel: string;
}>;
export default ResourceMetadata;
